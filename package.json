{
  "name": "bochechia-ops",
  "private": true,
  "version": "0.1.0",
  "devDependencies": {
    "next": "16.2.4",
    "react": "19.2.4",
    "react-dom": "19.2.4"
  },
  "scripts": {
    "dev": "npm --prefix dashboard run dev",
    "build": "npm --prefix dashboard run build",
    "lint": "npm --prefix dashboard run lint",
    "vercel-build": "npm --prefix dashboard run build",
    "validate:aios": "node scripts/validate-aios.cjs",
    "smoke:aios": "node scripts/smoke-runner.cjs",
    "check:aios": "npm run validate:aios && npm run smoke:aios"
  }
}
